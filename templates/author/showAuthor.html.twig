<!DOCTYPE html>
<html>
<head>
    <title>Détails de l'Auteur</title>
</head>
<body>
    <h1>Détails de l'Auteur</h1>
    {% if author %}
        <p>Nom : {{ author.username|upper }}</p>
        <p>Photo : <img src="{{ asset(author.picture) }}" alt="{{ author.username }}" width="100"></p>
        <p>Email : {{ author.email }}</p>
        <p>Nombre de livres : {{ author.nb_books }}</p>
    {% else %}
        <p>Aucun auteur trouvé.</p>
    {% endif %}
</body>
</html>