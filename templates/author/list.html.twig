<!DOCTYPE html>
<html>
<head>
    <title>Liste des Auteurs</title>
</head>
<body>
    <h1>Liste des Auteurs</h1>
    <p>Nombre total d'auteurs : {{ authors|length }}</p>
    {% if authors is defined and authors|length > 0 %}
        <ul>
            {% for author in authors %}
                <li>
                    <img src="{{ author.picture }}" alt="{{ author.username }}" width="100">
                    {{ author.username|upper }} ({{ author.email }}) - Livres : {{ author.nb_books }}
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>Aucun auteur disponible.</p>
    {% endif %}
</body>
</html>
